// function getStringWidth(num) {
//     let n = String(num);
//     return n.length;
// };

// function fillSpaces(str, width, plus) {
//     str = String(str)
//     if(plus != undefined) {
//         width += plus;
//     }

//     return str.length >= width ? str : new Array(width - str.length + 1).join(' ') + str;
// }

// module.exports.getStringWidth = getStringWidth;
// module.exports.fillSpaces = fillSpaces;